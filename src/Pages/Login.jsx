import { Link } from "react-router-dom";

const Login = () => {
    return (
        <div>
            Log in page
            <Link to='registration'>Registration</Link>
        </div>
    );
};

export default Login;